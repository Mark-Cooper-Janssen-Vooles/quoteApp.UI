import {render, screen} from "@testing-library/react";
import React from "react";
import SentQuotes from "./SentQuotes";

describe("SentQuotes", () => {
    test('renders component', () => {
        // render(<SentQuotes />);
        // expect(screen.getByTestId('sent-quotes')).toBeTruthy();
    });
})
